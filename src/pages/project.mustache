<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
        <meta name="description" content="{{descr}}"/>
        <meta name="author" content="Giacomo Parmeggiani"/>

        <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon"/>
        <link rel="icon" href="/favicon.ico" type="image/x-icon"/>

        <link rel="canonical" href="https://www.giacomoparmeggiani.com/projects/{{id}}.html"/>

        <title>{{prj.title}}</title>

        {{> partials/common_css }}
    </head>
    <body id="page-top">

        {{> partials/navigation }}

        <section class="bg-primary text-white mt-5">
            <div class="container">
                <h2 class="text-center text-uppercase text-white">{{prj.title}}</h2>
                {{#prj.subtitle}}
                <div class="row">
                    <div class="col-lg-12 text-center">
                        {{{prj.subtitle}}}
                    </div>
                </div>
                {{/prj.subtitle}}
            </div>
        </section>
        {{#prj.body}}
        <section>
            <div class="container project-container">
                {{{prj.body}}}
            </div>
        </section>
        {{/prj.body}}
        {{#prj.extra}}
        <section>
            <div class="container project-container">
                {{> extra_partial }}
            </div>
        </section>
        {{/prj.extra}}

        {{> partials/footer }}
        {{> partials/common_js }}
        <script>
            $('.nav-link.portfolio').addClass("active");
        </script>
    </body>
    </html>
